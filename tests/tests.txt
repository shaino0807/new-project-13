# Tests

## Test 22 - Missing snapshot and on-demand value score [regression]
Viewport: desktop
Description: Check honest empty states and selected-stock score loading.

Steps:
1. With no accepted ranking snapshot, open the homepage. It must show that the first verified full-market update is pending, without presenting a rejected legacy batch as newly updated.
2. Analyze 2330, open fundamentals, and click the button to load this stock's fundamentals score.
Expected: Only the selected stock is scored; real coverage, risk gauge and component bars update. No full-market refresh or raw database download is triggered. Missing inputs remain explicitly unavailable.

## Test 1 - Homepage waits for user input [sanity]
Viewport: desktop
Description: Verifies the public site no longer auto-loads a default stock before the visitor enters a code.

Steps:
1. Navigate to the app without a `code` query parameter.
2. Wait for the page to fully load.

Expected: The header and market-radar homepage are visible without a default stock analysis. It must not show 2330 台積電, a default close price, or a default pullback probability before user input. The homepage reads `/api/market-radar` or completed `/api/screener/snapshot` data only and must not call `/api/workbench`, `/api/value-score`, or silently start a new FinMind scoring batch.

## Test 2 - Visitor can analyze a stock by company name
Viewport: desktop
Description: Verifies a shared visitor can enter a stock name or code and receive backend quote analysis on the same page.

Steps:
1. Navigate to the app.
2. Type 鴻海 in the stock code or name field and wait for the visible stock directory list below the field.
3. Click the candidate labeled 2317 鴻海, then click the analysis button.
4. Wait for 股票完整分析 to open automatically after the analysis succeeds.
5. Confirm 今日結論, 追高風險, 支撐, 壓力, and 失效 are visible in the stock workspace.
6. Change monthly budget to 5000.

Expected: The name resolves to 2317 鴻海 using the Taiwan company directory, then the page shows the five stock sections 概覽, 價格／圖表, 基本面, 催化, and 計畫. Ranking and portfolio health are not duplicated in the stock deep page; they remain in 自選股與持股. An ambiguous partial name must show candidates instead of silently selecting a company. Budget-dependent sections update without reloading the page.

## Test 3 - Agent answers a follow-up question
Viewport: desktop
Description: Verifies the Agent panel answers questions about the currently loaded stock.

Steps:
1. Navigate to the app.
2. In the top hero stock code/name field, enter 2330 and click 分析; do not click the Dino doorway card.
3. Wait for the successful search to open 股票完整分析 automatically.
4. Click the 前往互動 Agent shortcut at the top of that page.
5. Confirm the page scrolls to the section titled 互動分析 Agent, then click the 支撐壓力 prompt inside that section.
6. Wait for the Agent response.

Expected: The Agent log shows the user's question and an Agent answer with key points, actions, watchlist items, and a disclaimer.

## Test 4 - Primary chart and selectable indicators remain usable
Viewport: desktop
Description: Verifies the existing Chart.js data remains available without stacking every auxiliary chart.

Steps:
1. Navigate to the app.
2. Type 2454 in the stock code field.
3. Click the analysis button.
4. Wait until the successful search automatically opens the stock deep page.
5. Click 前往價格圖表 and confirm the primary price chart is visible near the start of 價格／圖表.
6. Switch the 技術副圖 tabs through 成交量, MACD, RSI / KD, and Bollinger using both pointer and ArrowLeft / ArrowRight keys.
7. Open the current chart's text/data alternative, then expand 進階技術理論與訊號證據.

Expected: Price and support/resistance remain visible. Exactly one of volume, MACD, RSI/KD, and Bollinger is displayed at a time; the selected tab has `aria-selected=true`, its panel is visible, and the chart resizes after selection. The text summary and latest-value table update with the selected indicator. Elliott Wave / Chan, Granville, Dow Theory, and twelve-dimensional signals are collapsed by default but remain available after expansion. No OHLC or candlestick chart is introduced.

## Test 5 - Mobile first screen prioritizes the decision
Viewport: mobile
Description: Verifies the Agent presents the decision immediately after stock selection without shrinking controls or evidence into unreadable text.

Steps:
1. Navigate to the app on a mobile-width viewport.
2. At both 390 × 844 and 320 × 844, confirm the stock input and the three primary buttons 自選股, 策略選股, and 風險掃描 remain visible and readable.
3. Open each primary button by touch and return to the homepage; no hamburger-only or desktop-only path may hide them.
4. Enter 0050 and run analysis. Confirm the successful search opens 股票完整分析 automatically and the five decision cards remain readable without horizontal page overflow.
5. Use the five-item sticky section navigation.
6. Return home, open 策略選股, and inspect the candidate list at 390px and 320px widths.

Expected: The mobile homepage keeps all three primary destinations reachable without hiding `.market-header-actions`. A successful search opens full analysis, while a failed search leaves the user on the current page with an actionable message. In full analysis, the five sticky section links remain horizontally usable, 價格／圖表 is reachable directly after 概覽, and one auxiliary chart is visible at a time. Every visible interactive target is at least 44 × 44 CSS pixels, no visible text is below 12 CSS pixels, and the page has no horizontal overflow. General dense tables scroll inside their panels when needed, while Dino candidates render as labeled mobile cards without hiding rank, evidence, invalidation, or selection controls.

## Test 6 - Macro news degrades without fabrication
Viewport: desktop
Description: Verifies the macro/news panel distinguishes readable evidence from an unavailable upstream source without blocking Dino.

Steps:
1. Navigate to the app.
2. From the compact homepage navigation, click 策略選股 exactly once and wait until the workspace heading 1. 總經與最新新聞 is visible; do not click a hidden or already-active workspace tab.
3. Wait for that section to either render news cards or show its explicit timeout/unavailable state, independently of any stock search.
4. If news cards are present, inspect their source links and whether the UI says article content or only RSS material was available. If no cards are present, inspect the unavailable explanation instead and stop.

Expected: The section lists macro news or clearly states timeout/unavailable status. Every returned item has a traceable source and explicitly distinguishes original-article reading from RSS-only evidence; RSS-only material cannot independently drive a conclusion. When no item is returned, the UI shows the upstream limitation and does not invent a news item, source, or Evidence ID. The screener remains independently usable.

## Test 7 - Fundamentals load for a new stock
Viewport: desktop
Description: Verifies each stock search requests the aggregated backend route and replaces placeholder fundamentals.

Steps:
1. Navigate to the app.
2. Use the stock code/name field in the compact header, not the lower Agent question field, to run 2330 analysis.
3. Wait for fundamentals to finish loading.
4. Use the same compact-header stock code/name field and the analysis button, not the Agent question button, to run 2344 analysis.
5. Wait for fundamentals to update to the new stock.

Expected: Available rows include numeric values and source dates. In Dino deep analysis, the fundamental section shows period, current value, comparison value, change, interpretation, and source for monthly revenue, EPS/profitability, cash flow, and balance-sheet evidence. A complete fundamental conclusion requires the visible minimum evidence gate; unavailable upstream data is marked clearly instead of inventing values.

## Test 8 - Ranking refresh uses a completed evidence-gated snapshot
Viewport: desktop
Description: Verifies quota, authorization, maintenance, or upstream failures do not break existing features.

Steps:
1. Navigate to the app.
2. Open 自選股與持股, then locate its ranking controls.
3. Click 更新工作台 once and wait for the ranking list or its explicit unavailable state.
4. Switch between 低估, 高估, 現金流, and 成長 ranking tabs.

Expected: Ranking and holding workflows are visible in 自選股與持股 and are not duplicated in 股票完整分析. Updating the workbench reads the available persisted data or clearly reports an unavailable state without breaking the stock page. Switching ranking tabs reads existing results and does not require a stock search or silently start a new full-market scoring batch.

## Test 9 - 0050 uses ETF-specific handling
Viewport: desktop
Description: Verifies an ETF is not evaluated as if it were a single listed company.

Steps:
1. Navigate to the app.
2. Type 0050 and run analysis.
3. Wait for fundamentals and news to finish loading.

Expected: ETF-specific rows, NAV/component data or clear official-source unavailable messages are shown. Company financial-statement placeholders must not be used for ETF-only fields.

## Test 10 - Dino starts resilient staged analysis
Viewport: desktop
Description: Verifies Dino exposes evidence-gated market context and starts a persistent deep-analysis job without an immediate network failure.

Steps:
1. Navigate to the app and click the homepage 策略選股 navigation button once.
2. Wait for macro/news, the 1-3 month screener, source panel, and either a complete thematic snapshot or the explicit full-market-building state.
3. Confirm the legacy 16-stock theme rearrangement is not shown as a full-market result while the first valid snapshot is building.
4. Enter the inactive/nonexistent code 2334 in the manual selection field and click 進入深度流程.
5. Confirm the page says that 2334 is not a currently valid listed/OTC target and does not create a job, call the AI report chain, or render repeated Yahoo 404/N/A tables.
6. Replace it with 2330 and click 進入深度流程.
7. Wait until the customer progress tray shows 請稍候, a percentage, and a short customer-facing status; do not wait for every research stage or the final report in this smoke test.

Expected: Dino can load before a stock is selected. A completed thematic snapshot contains five independently scanned full-market Top 10 tables with coverage, reason, date/source, and invalidation fields; otherwise an honest building state is shown. The numeric input path validates current market membership instead of accepting any four digits. Invalid 2334 is rejected before job creation with a clear Chinese explanation. Starting valid 2330 creates or resumes the staged report job and displays percentage progress instead of an immediate Network Error. The customer UI must not expose a job ID, Skill path, internal stage key, internal success/failure count, or orchestration table. A result is shown only when its required structured evidence passes; unavailable evidence cannot be labeled generated merely because AI prose exists. Long valuation, fundamental, technical, signal, and synthesis stages continue through the resumable job contract outside this short browser smoke test.

## Test 11 - Keyboard, focus, and workspace history remain coherent
Viewport: desktop and mobile
Description: Verifies the revised progressive workflow is operable without a pointer and preserves browser navigation.

Steps:
1. Navigate to the app without a stock and use Tab to reach the compact header stock input and 分析 button.
2. Confirm the market radar ranking and 策略選股 remain available before a stock is selected.
3. Analyze 2330 by keyboard, confirm success opens 股票完整分析, then use ArrowRight and ArrowLeft on the workspace tab list.
4. Confirm focus and `aria-selected` move together across visible tabs.
5. In 股票完整分析, use the five section links and then use ArrowRight and ArrowLeft on the 技術副圖 tab list.
6. Press the browser Back control or activate 返回首頁, then press browser Forward when available.
7. Enable reduced motion and increased contrast preferences, then repeat the main navigation.

Expected: Every interactive control has a visible focus indicator, status changes are announced through live regions, disabled entries do not receive accidental activation, and the workspace URL hash matches the active workspace tab. Section links scroll without replacing that workspace hash. The indicator tab focus and `aria-selected` state move together. Browser Back returns to the homepage rather than trapping the visitor, while Forward restores the prior workspace view. Reduced motion removes reveal/status animation without hiding content, and increased contrast keeps borders and focus rings visible.

## Test 12 - Customer progress remains readable without internal workflow leakage
Viewport: desktop and mobile
Description: Verifies long-running customer operations use a compact progress treatment instead of exposing implementation details.

Steps:
1. Navigate to the app and run a stock analysis.
2. Start or resume a ranking refresh or strategy deep analysis.
3. Inspect the fixed customer task tray and the inline deep-analysis waiting state at desktop and 390 x 844.

Expected: The customer sees 請稍候 or a plain-language task title, a percentage, one short status sentence, and whether leaving the page is safe. The progress bar and text remain legible with a contrast ratio of at least 4.5:1. No job ID, Skill name/path, internal phase key, orchestration contract, internal success/failure count, or raw backend message is visible.

## Test 13 - Network failures are actionable
Viewport: desktop and mobile
Description: Verifies a browser-level fetch failure does not expose a vague English error or fabricate stale stock data.

Steps:
1. Block the AppDeploy API host or simulate a rejected `fetch`.
2. Enter 2330 and start analysis.
3. Inspect the live status region and decision summary.
4. Restore the API connection and retry.

Expected: The status explains in Chinese that the analysis service cannot currently be reached and asks the user to check the network or retry later. It must not show only `Failed to fetch`, must not invent a quote, and must keep the full-analysis action disabled. After connectivity is restored, retrying 2330 completes normally.

## Test 14 - Macro risk scan preserves the complete prompt contract
Viewport: desktop and mobile
Description: Verifies the separate macro-risk workspace keeps all 17 indicators, all six report parts, strict evidence rules, and resumable refresh behavior.

Steps:
1. Navigate to the app without selecting a stock, then open 風險掃描 from the compact homepage navigation or workspace tab.
2. Confirm the page renders five short-term, seven medium-term, and five long-term indicators, grouped in that order.
3. Confirm each row shows current value or `--`, data date or an explicit unavailable state, fixed threshold, signal, direction versus the prior snapshot, interpretation, limitation, and a traceable source link.
4. Confirm the three horizon cards show 1-3 個月, 3-12 個月, and 1-3 年+, including the count of unalerted, red, yellow, and unknown indicators.
5. Inspect the 17 short interpretation cards, the three horizon conclusions, the seven hard-trigger cards, the daily focus item, and the centralized stale/restricted/source-conflict list.
6. Click 開始 / 續跑宏觀掃描 once, interrupt or reload after at least one persisted batch, then click the same button again.
7. At 390 × 844 and 320 × 844, inspect the full page and horizontally scroll only the 17-indicator table region.
8. Return to Dino K線選股 and inspect its macro context.

Expected: The contract remains exactly 5 + 7 + 5 = 17 indicators and seven hard triggers. Missing, stale, restricted, or unverified values remain unknown and are never counted as safe, unalerted, or not triggered. Daily inputs enforce the T-1/most-recent-trading-day rule, while monthly and quarterly indicators retain their permitted cadence and explicit dates. The page states 今日共有 X / 7 項觸發 and treats three or more as the phased-selling action threshold; two or more raises the warning level. Refresh work advances in small persisted batches, resumes after reload, and only replaces the visible snapshot after all 17 sources have been attempted. Dino receives only the read-only snapshot summary and does not change the original Dino regime score. The mobile page has no page-level horizontal overflow, the table scrolls internally, and every main control remains at least 44 CSS pixels high. No PDF control or PDF-generation request is present.

## Test 15 - Macro hard rules and confidence policy are deterministic
Viewport: desktop and API contract inspection
Description: Verifies the formerly ambiguous VIX, A/D, focus, BofA, and public-action rules cannot drift between scans.

Steps:
1. Inspect a VIX observation with two distinct consecutive source trading dates above 25, then repeat with only one date above 25 and with two same-day snapshots.
2. Inspect an A/D sequence where S&P 500 makes a 60-session closing high while A/D is at least 1% below its 60-session high; test one, two, and three consecutive qualifying sessions.
3. Inspect focus selection with at least 253 daily observations, 53 weekly observations, and 37 monthly observations. Then repeat with shorter histories.
4. Inspect a BofA observation marked A, B, C, and D. For C, test one source, two independent current-week sources agreeing within 0.1, and two sources outside that tolerance.
5. Inspect the macro-risk page table, trigger cards, focus section, and limitations section.
6. Inspect `docs/macro-risk-data-sources-todo.md` without enabling credentials or purchasing any source.

Expected: VIX hard-trigger status is true only after two different consecutive source trading-session closes above 25, never because a user reran the same-day scan. A/D hard-trigger status is true only after all of: 60-session S&P closing high, A/D at least 1% below its own 60-session closing high, and three consecutive qualifying sessions; one or two sessions are at most near. Focus uses absolute one-period historical Z-score and selects the largest score only when it is at least 2.0; otherwise it reports the nearest normalized fixed threshold and its fallback method. BofA accepts A/B, or only two qualifying C sources, and all other evidence remains unknown. The public page does not state a fixed sell percentage: three of seven triggers starts a staged risk-reduction review. The purchase TODO lists NAAIM, AAII, GuruFocus, and BofA with licensing, data-quality, and regression gates, while no paid credential is configured.

## Test 16 - Ranking trust contract, sorting, and responsive presentation
Viewport: desktop, 390 x 844, and 320 x 844
Description: Verifies the customer ranking is explicit about its controlled batch, freshness, eligibility, deterministic ordering, and compact mobile reading path.

Steps:
1. Navigate to the homepage market radar. Wait for the ranking status strip or the explicit unavailable state; then open 自選股 if the detailed workbench needs inspection.
2. Inspect 本次評分, 可列入排名, 高信賴度, and 更新時間. Confirm the note also states the quote-date window and controlled-batch scope.
3. Keep 資料品質 on 可列入排名. Switch between 低估, 高估, 現金流, 成長, and 小資; for an equal primary score, confirm the tie order is data coverage, then quote date, then stock code.
4. Change 資料品質 to 高信賴度 and then 包含資料受限列. Confirm limited rows display a data-limited state and receive no fabricated rank.
5. Change 顯示排序 to 資料覆蓋率, 報價日期, and 股票代碼. Confirm the table order changes without requesting a new stock-analysis batch.
6. At desktop width, vertically scroll the dense ranking panel and horizontally scroll it if needed. At 390 x 844 and 320 x 844, confirm it changes to labeled cards.

Expected: The ranking states that it is not a real-time full-market leaderboard. A row is rankable only with a score, quote date, at least 60% coverage, and no stale dataset; high trust additionally requires 85% coverage. The primary-score order is deterministic and does not depend on upstream return order. Rank, stock, and the table header stay readable on desktop; the two fixed identity columns do not obscure the remaining fields. Mobile has no page-level horizontal overflow, exposes status, score, coverage, and quote date in each card, and keeps every control at least 44 CSS pixels high.

## Test 17 - Market radar industry, left-right strategy, and honest scope
Viewport: desktop, 390 x 844, and 320 x 844
Description: Verifies the approved light market-radar homepage uses the current controlled batch for industry and strategy research without pretending to be TradingView or a full-market real-time feed.

Steps:
1. Navigate to the homepage and wait for the latest complete snapshot.
2. Inspect the batch summary, industry heatmap, filter controls, ranking table, and mobile ranking cards.
3. Select one industry tile and confirm the industry select and ranking rows update together without a new upstream scoring request.
4. Switch strategy between 綜合研究, 左側交易, and 右側交易. Confirm each row shows the selected strategy label and a reproducible match score.
5. Switch event filters between 低估值得研究, 成長轉強, 現金流較完整, 趨勢確認, and 追高風險.
6. Select a ranking row and confirm it opens the existing stock analysis path.
7. Reload the page and confirm the browser-local filter preference is restored.
8. At mobile width, confirm the industry tiles scroll horizontally, filters use a disclosure panel, and ranking rows become labeled cards.

Expected: The heatmap aggregates the current controlled batch's actual one-day quote change percentages by the site's industry group and states that it is not full-market breadth. Left-side matching weighs undervaluation, valuation, cash flow, financial stability, and avoiding chase risk; right-side matching separately weighs trend, market confidence, growth, profitability, cash flow, and avoiding chase risk. These scores are labeled as first-pass research matching, not buy signals. Box, breakout, fakeout, invalidation, and long/short conclusions remain in the existing K-line deep-analysis path. No TradingView or Dipping.ai data or proprietary formula is imported, copied, or implied.

## Test 18 - Each ranking independently selects Top 64 from its declared universe
Viewport: API contract and desktop
Description: Prevents the old fixed-64 loop from returning under a different label.

Steps:
1. Run `node scripts/ranking-v2-pilot.mjs --live-universe` against the public company directory.
2. Confirm the listed/OTC source directory contains at least 1,700 companies and the current expected directory contains 1,985 (TWSE 1,133 + TPEx 852); emerging companies are excluded.
3. Read the latest TWSE and TPEx official bulk quote tables, assign within-market high/medium/low liquidity tiers, and confirm the deterministic pilot contains 250 unique companies across market, official industry, and liquidity strata. Reversing the directory input must produce the same sample.
4. Build each of undervalued, overvalued, active, cashflow, growth, and small-investor rankings from all 250 pilot feature rows.
5. Inspect the publication path and confirm it loads all persisted feature rows before mode-specific sorting; it must not publish from the temporary candidate union.
6. Compare the six Top 64 constituent sets and deterministic tie order.

Expected: Every mode independently applies its own score to the same declared universe, filters for score + quote date + at least 60% coverage + no stale dataset, sorts by primary score → coverage → quote date → code, and returns at most its own Top 64. At least two synthetic mode constituent sets differ. No shared temporary candidate pool remains. The 250-stock output is labeled a performance pilot; only a completed 1,985-company scan may be labeled the listed/OTC full-market snapshot. Opening the homepage reads persisted snapshots and performs zero stock rescoring calls. A completed real pilot reports wall and active time, exact upstream request totals by provider, cache/inflight hits, response bytes, retry attempts, final failures, quote-date/coverage/no-stale/rankable completeness, and projected 1,985-stock time and request volume without activating a public full-market snapshot.

Legacy snapshot regression: replace the active snapshot fixture with rows at 55-59% coverage or any stale dataset, then call `GET /api/screener/snapshot` and `GET /api/market-radar`. Expected: persisted rows are revalidated under the current policy; the snapshot returns `snapshotStatus: rejected`, displays zero ranking rows, and the homepage reports that no currently valid completed ranking is available without starting a new scoring job.

## Test 19 - Market-aware quote routing, official daily cache, and terminal outcomes
Viewport: API contract and local source regression
Description: Prevents expected wrong-market Yahoo 404s, repeated official bulk downloads, and ambiguous pilot failure totals.

Steps:
1. Run `node scripts/ranking-v2-pilot.mjs` and confirm the source-contract checks pass.
2. Confirm a TWSE company profile produces only the Yahoo `.TW` suffix and a TPEx profile produces only `.TWO`; only an unknown market may retain a controlled two-suffix fallback.
3. Request multiple stocks from the same market during one Taipei calendar day and confirm the official TWSE or TPEx bulk quote payload is fetched once, then served from cache or the shared in-flight request. For a persisted 250-stock pilot, confirm the selected company profiles retain their official quote rows across advance calls and those rows avoid redundant per-stock MIS requests.
4. Exercise an explicit exchange suspension/halt status, a halt record whose official resumption date precedes the quote date, a normally returned market snapshot with no usable stock quote, an upstream timeout or invalid payload, and a successfully scored row below the publication evidence gate.
5. Inspect the completed pilot performance summary and sample fingerprint.

Expected: Explicit official halt evidence maps to `suspended_or_halted`, but a historical halt record with a resumption date on or before the quote date does not; a healthy correct-market snapshot without a usable latest-session quote maps to `no_quote_for_latest_session`; transport, HTTP 5xx, timeout, and invalid payload errors map to `provider_failure`; successfully scored rows that fail ranking evidence map to `not_rankable`; insufficient evidence remains `unresolved`. A quote marked suspended or missing for the latest official session is not rankable merely because older Yahoo history exists. The summary reports mutually auditable terminal counts, the fixed selection algorithm emits a sample fingerprint, cache and in-flight hits remain separate from real HTTP request totals, and no pilot result activates a public ranking snapshot.

## Test 20 - Full 1,985-stock precision and runtime contract
Covers: /api/screener/snapshot evidence rejection; internal ranking contracts and history persistence (local tests only).
Viewport: local backend/source contract; no deployment
Description: Verifies the full-market path reduces repeated requests without weakening evidence, disposition, or cutoff rules.

Numeric evidence regression: Run `node scripts/test-ranking-numeric-evidence.mjs`. The production backend contract must reject null, undefined, blank strings, booleans, arrays, objects, and non-finite values for every numeric requirement in all six modes. Zero, negative finite values, and finite numeric strings remain valid evidence. Undervalued and overvalued must require two genuinely numeric valuation fields. This isolated check performs no network requests or persistent writes.

Phase D local preflight: Run `node scripts/test-phase-d-preflight.mjs`. Formal ranking must use both official company directories, reject stale/missing/mixed source dates and duplicate codes, exclude DR/fund products, and preserve legitimate common shares. Expired or incompatible jobs must not resume. UTF-8 serialized item and batch limits must reject oversized records before database writes. This local test does not authorize deployment or any remote job.

Phase D storage regression: Run `node scripts/test-ranking-storage.mjs`. Verify production persistence functions round-trip 1,976 and 6,000 company fixtures and large result/history/snapshot fields under SDK byte limits. Partial or missing chunks must fail closed; unchanged chunks must be reused. Simulate interruption before job progress commits and loss of acknowledgement after it commits; resuming must not double-count scores. A missing referenced feature batch must abort publication preparation. Partial six-mode snapshot writes and pilot runs must not activate public ranking pointers. The read-only storage audit must measure persisted bytes and reject duplicate, missing or malformed history rows. Tests use database doubles and do not establish remote quotas or distributed concurrency safety.

Cache provenance acceptance (internal API diagnostics): After warming 2330 through GET /api/fundamentals?code=2330, repeat after the backend update. Inspect cache.datasetSources: persistent financial datasets must report source=persistent and stale=false, preserving fetchedAt from before the update. source=memory alone does not prove database readback. Missing or stale evidence remains a failed acceptance condition; this check does not authorize advancing ranking jobs.

Steps:
1. Run `node --experimental-strip-types --check backend/index.ts` and `node scripts/ranking-v2-pilot.mjs` without `--live-universe`.
2. Confirm the simulation contains exactly 1,985 stocks, uses batches of at most 20 with concurrency 4, and therefore schedules 100 scoring batches rather than one request chain per stock.
3. Inspect the official-history phase: TWSE and TPEx market-day payloads are persisted by market/date until each market has 120 valid sessions, then transposed into per-20-code history batches. A ranking job must stop if fewer than 60 verified sessions exist and must never substitute generated price history.
4. Confirm daily quote and monthly revenue routes select TWSE or TPEx directly. Confirm authenticated FinMind financial statement, cash-flow, balance-sheet, and monthly-revenue results have persistent daily cache records; no token is printed or embedded.
5. For undervalued, overvalued, active, cashflow, growth, and small-investor modes, remove one mandatory field at a time and confirm the row is excluded from that mode. In particular, remove ending cash from cashflow and EPS YoY from growth.
6. For the 20MA theme, independently fail each of: close above a rising 20MA, downward-resistance trend-line breakout, W-bottom neckline breakout, and volume at least 1.5 times the preceding 20-session average.
7. Reverse the entire 1,985-row input and compare each mode's ordered eligible set and retained ranks 50-90; inspect rank 64 and rank 65.
8. Exercise `provider_failure`, `unresolved`, `no_quote_for_latest_session`, `suspended_or_halted`, and `not_rankable` terminal categories.

Expected: All six modes return exactly 64 synthetic rows only when their own required-field contracts pass. Reversing input does not alter deterministic order, and every mode retains a 41-row rank 50-90 boundary audit. Missing mandatory fields and any failed 20MA condition are rejected rather than assigned a plausible-looking rank. Only `provider_failure` is retried; deterministic no-quote, suspension, and evidence failures are not. A formal snapshot cannot activate while any `provider_failure` or `unresolved` disposition remains, even if the numerical 95% scored threshold was reached. This test proves local orchestration and ranking contracts only; it does not claim a live 1,985-stock collection, remote storage-capacity pass, deployment, or public activation.
Phase D calendar regression: verified 2026 scheduled and typhoon closures must be skipped without synthetic bars; preserve prior raw errors as verified closure evidence. Unknown dates and provider failures must still stop the runner. Run node scripts/test-phase-d-preflight.mjs.
Phase D recovery: retry only a recorded failed history date, at most twice per key. Verify persisted day identity before clearing the error, retain resolution evidence, never double-count sessions, and never automatically retry quota failures.
Phase D runtime budget: history advances at most one candidate day (two markets), with an eight-second timeout per history source. Existing saved days are reused after an interrupted response; no duplicate session counts.
Phase D diagnostics: feature-audit reads only the job-referenced named feature batch, never fetches upstream or writes data, returns bounded existing public evidence warnings, and rejects unknown batch keys.
FinMind recovery regression: run node scripts/test-finmind-recovery.mjs. The configured token is used on the first request and is never downgraded to anonymous after rejection. 402/429 and 401/403 stop without identity switching; transient errors retry at most once. Recovered attempts do not block the runner; unresolved dataset failures do, including logical errors in HTTP 200 payloads. Structured diagnostics contain no token or raw response body.
Test 21: Visual analysis dashboards and backend-only FinMind
Open stock, portfolio, swing, and macro risk tabs at desktop, 390px and 320px widths. Verify gauge labels, numeric scales, readable bars, no horizontal document overflow, and keyboard access. With missing values, gauges must show unavailable and no needle; a real zero must remain zero. With reduced-motion enabled, needles and bars must not animate. Query 2330 and verify the stock sparkline uses the returned closes and the RSI gauge matches the textual RSI. Portfolio charts must reflect entered weights and must not invent profit/loss. Confirm there is no data-download tab, CSV button or /api/data-library route. FinMind credentials and raw dataset downloads must not appear in the browser.
